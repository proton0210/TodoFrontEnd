version: 1
runtime: nodejs14
build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build
  env:
    - name: NODE_ENV
      value: production
run:
  runtime-version: 14.x
  command: npm run preview --port $PORT
  network:
    port: 8080
    env: PORT
