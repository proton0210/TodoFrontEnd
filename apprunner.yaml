version: 1
runtime: nodejs14
build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build
run:
  runtime-version: 14.21.13
  command: npm run preview --port $PORT
  network:
    port: 8080
    env: PORT
