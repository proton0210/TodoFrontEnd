version: 1
runtime: nodejs14
build:
  commands:
    - npm install
    - npm run build
run:
  command: npm run preview -- --port $PORT
  port: 8080
  environment:
    - NODE_ENV=production
    - HOST=0.0.0.0
